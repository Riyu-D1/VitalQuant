# Local dev (docker-compose db). For Supabase, paste the project connection string.
DATABASE_URL=postgresql://vitalq:vitalq@localhost:5432/vitalq
VITALQ_AUTH_MODE=local           # 'supabase' requires JWKS wiring — see ingest/auth.py
VITALQ_DEVICE_KEY=               # printed once by `vitalq-device --label ...`
